SELECT SNO, SNAME, RESULT
FROM STUDENT
JOIN SCORE USING(SNO)
WHERE RESULT > 60;

SELECT *
FROM STUDENT;

SELECT *
FROM PROFESSOR;

SELECT *
FROM COURSE;

SELECT CNO, CNAME, RESULT, PNO, PNAME
FROM STUDENT
JOIN SCORE USING(SNO)
JOIN COURSE USING(PNO);
JOIN PROFESSOR USING(CNO);
